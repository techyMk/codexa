services:
  backend:
    build: ./backend
    container_name: codexa-backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    restart: unless-stopped

  # Frontend is run via `npm run dev` for fast HMR.
  # Bring it under compose only if you need a fully containerized stack.
